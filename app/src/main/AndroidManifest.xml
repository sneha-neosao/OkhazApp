<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ok_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/ok_icon"
        android:supportsRtl="false"
        android:theme="@style/Theme.Delievrys"
        tools:replace="android:icon,android:supportsRtl">
        <activity android:name="ae.okhaz.boss.view.activities.ProductListActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar"
            />
        <activity
            android:name="ae.okhaz.boss.view.activities.SignUpActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.AddDeliveryBoyActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.EditDeliveryBoyActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.MainDrawerBackActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.ForgotPasswordActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.ResetPasswordActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.GetStartedActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.DeliveryBoyDetailsActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.view.activities.AdminDetailActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.NotificationActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.TutorialActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="ae.okhaz.boss.Activitys.Supplier.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="ae.okhaz.boss.view.activities.Maps1Activity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="ae.okhaz.boss.Activitys.Supplier.DeliveryBoyActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="ae.okhaz.boss.Activitys.Supplier.DeliveryBoyListActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.EditDetailsActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.Supplier.SupplierActivity"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Delievrys.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ae.okhaz.boss.Activitys.LoginActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity
            android:name="ae.okhaz.boss.Activitys.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Delievrys.NoActionBar" />
        <activity android:name="ae.okhaz.boss.ImagePickerActivity" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <service
            android:name="ae.okhaz.boss.FCM.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>