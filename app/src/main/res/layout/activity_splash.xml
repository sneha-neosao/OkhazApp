<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    >

  <ImageView
      android:layout_width="fill_parent"
      android:layout_height="match_parent"
      android:scaleType="centerCrop"
      android:src="@drawable/okhaz_splash"
      />

  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginBottom="95dp"
      android:layout_marginLeft="90dp"
      android:layout_alignParentBottom="true"
    android:orientation="vertical">

<ImageView
    android:layout_width="90dp"
    android:layout_height="90dp"
    android:layout_gravity="center"
  android:src="@drawable/ok_logo"/>

    <com.ncorti.slidetoact.SlideToActView
        android:id="@+id/btn_start_slide"
        android:layout_width="190dp"
        android:layout_height="40dp"
        android:elevation="6dp"
        app:area_margin="5dp"
        android:visibility="visible"
        app:slider_icon="@drawable/ic_baseline_keyboard_arrow_right_24"
        app:icon_margin="4dp"
        android:layout_marginBottom="10dp"
        app:animation_duration="250"
        app:outer_color="@color/colorprimary"
        app:inner_color="@color/white"
        app:text="Swipe to Start"
        app:text_size="18sp"
        android:layout_gravity="center"
        android:layout_weight="2"
        app:slider_height="40dp"
        app:slider_locked="false" />


  </LinearLayout>

</RelativeLayout>